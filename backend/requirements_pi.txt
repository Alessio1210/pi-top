# Pi-Top Spezifische Requirements
# OpenCV und Dlib werden via apt-get installiert (python3-opencv, python3-dlib)
# Daher hier NICHT in der Liste, um das lange "Building wheel" zu verhindern.

flask>=3.0.0
flask-cors>=4.0.0
supabase>=2.0.0
python-dotenv>=1.0.0
pyserial>=3.5
Pillow>=10.0.0
python-dateutil>=2.8.0
requests>=2.31.0
numpy>=1.21.0
gpiozero>=2.0
smbus2>=0.4.0
# rpi_lcd # Optional, falls Standard 16x2 Display


# Face Recognition manuell behandeln wir im Befehl
# face-recognition
